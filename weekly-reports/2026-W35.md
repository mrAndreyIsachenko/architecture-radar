# Weekly Synthesis: 2026-W35

## Week And Scope
- Week id: `2026-W35`, synthesized on 2026-08-24 from committed artifacts present in the repository.
- Scope stayed read-only over local committed artifacts only: `interests.md`, `docs/research-scope.md`, `radar.json`, `reports/*.md`, `repositories/*.md`, `patterns/*.md`, `opportunity-interests.md`, `opportunity-reports/*.md`, `opportunities/*.md`, and `opportunities.json`.
- Opportunity state matters this week because `opportunities.json.selected` is empty; that means there is no active Opportunity Radar experiment to carry forward.

## Input Reports
- Architecture Radar reports reviewed for the week synthesis: `reports/2026-08-20.md` and `reports/2026-08-23.md`, with earlier reports used as background context.
- Opportunity Radar reports reviewed for demand-state context: `opportunity-reports/2026-08-18.md` and `opportunity-reports/2026-08-22-cashflow-reevaluation.md`.
- Supporting files reviewed for synthesis: `interests.md`, `docs/research-scope.md`, `radar.json`, `opportunity-interests.md`, `opportunities.json`, `patterns/evidence-carrying-execution-envelopes.md`, `patterns/deferred-image-materialization.md`, and `patterns/reorg-safe-materialization-windows.md`.

## Executive Synthesis
- The committed evidence moved further toward explicit state boundaries rather than larger or fancier systems: the new reviews all prefer persisted state, reversible promotion, or wire-confirmed reconciliation over inferred progress.
- `ai-llm-systems` gained a fresh mechanism in `repositories/RightNow-AI-autokernel.md`, where a benchmark-gated optimization loop only advances after deterministic setup, repeatable correctness checks, and persisted orchestration state.
- `document-ai-ocr` strengthened again through `repositories/run-llama-liteparse.md`, which keeps structural parsing authoritative and treats screenshots or block exports as optional promotions.
- `blockchain-intelligence` now has a tighter finality split in `repositories/chaindexing-chaindexing-rs.md`, where indexing finality and side-effect finality are separated and reorg repair is written back through Postgres.
- `privacy-networking-vpn` is represented this week by `repositories/openziti-ziti.md`, which shows that reconnect reconciliation is safer when the controller gets a full-refresh announcement with wire-level confirmation.
- `drones-robotics-autonomy` now has a stronger evidence trail in `repositories/alireza787b-mavsdk_drone_show.md`, where mission admission, durable journals, and read-only evidence bundles are separated instead of collapsed into one mutable status blob.

## Pattern Movement
- `Deferred Image Materialization` gained another reinforcing implementation in `repositories/run-llama-liteparse.md`; the new review keeps rendering and screenshot promotion optional instead of eager.
- `Reorg-Safe Materialization Windows` gained another concrete implementation in `repositories/chaindexing-chaindexing-rs.md`; the interesting boundary is now clearly between indexed history and side effects that should wait for finality.
- `Evidence-Carrying Execution Envelopes` gained a broader durable-workflow variant in `repositories/alireza787b-mavsdk_drone_show.md`; the journal and evidence bundle make command lifecycle state auditable after restart.
- `openziti/ziti` does not add a named pattern file, but it does reinforce the same general movement: reconcile stale control-plane state with an explicit full refresh rather than an optimistic incremental retry.

## Topic Coverage
- `ai-llm-systems` is the strongest family this week because it now spans both durable agent-style orchestration and benchmark-gated state transitions.
- `document-ai-ocr` is solidly covered, with the main question shifting from parsing capability to how much optional promotion changes output shape and runtime cost.
- `blockchain-intelligence` is now well supported on the reorg and repair side, but it still needs more evidence before higher-level transaction interpretation should be treated as mature.
- `privacy-networking-vpn` is thinner than the other fresh families because this week contributes one strong control-plane recovery mechanism rather than a broader cluster of variants.
- `drones-robotics-autonomy` is still narrower than the AI and document families, but the new journal/evidence split is materially stronger than last week’s mostly background coverage.

## Repeated Candidates Or Signals
- Suppress the older cooldown candidates from the prior synthesis unless a material head change appears: `wso2/labs-agentic-engineer`, `originrouter/originrouter_cli`, `DeusData/codebase-memory-mcp`, `cocoindex-io/cocoindex`, `firezone/firezone`, `netbirdio/netbird`, `PX4/PX4-Autopilot`, and `mavlink/qgroundcontrol`.
- Revisit the older repeated-document and blockchain comparison set only when there is a genuine new mechanism or head change: `run-llama/ParseBench`, `datalab-to/marker`, `docling-project/docling`, `bitcoincore-dev/nakamoto-electrs`, `trezor/blockbook`, and `graphprotocol/graph-node`.
- Keep the Opportunity Radar hypotheses in non-active status: `opportunities/langgraph-checkpoint-persistence-and-cost-diagnostics.md`, `opportunities/tailscale-split-tunnel-routing-exception-diagnostics.md`, and `opportunities/ardupilot-telemetry-integrity-and-failsafe-diagnostics.md` remain watchlisted or blocked, not selected.

## Decisions And Experiments
- Architecture decision: keep the week centered on explicit recovery boundaries and reversible promotion, anchored by `patterns/evidence-carrying-execution-envelopes.md`, `patterns/deferred-image-materialization.md`, and `patterns/reorg-safe-materialization-windows.md`.
- Opportunity decision: no active Opportunity Radar experiment is recommended this week because `opportunities.json.selected` is empty; the current watchlisted items should stay in evidence-gathering mode only.

## Evidence Gaps
- `repositories/RightNow-AI-autokernel.md` still needs workload-specific validation outside the benchmark harness before the move-on thresholds should be trusted broadly.
- `repositories/run-llama-liteparse.md` still needs backend and model-matrix validation across its optional OCR and conversion paths.
- `repositories/chaindexing-chaindexing-rs.md` still needs live chain and Postgres validation to confirm fork-point repair under production load.
- `repositories/openziti-ziti.md` still needs controller-load validation to prove the reconnect refresh path behaves under backlog and partial delivery.
- `repositories/alireza787b-mavsdk_drone_show.md` still needs SITL or fleet-restart validation to prove the journal and evidence bundle remain consistent under real mission fan-out.
- The Opportunity Radar side still lacks a selected experiment, so there is no buyer-confirmed next-step to promote from watchlist to active work.

## Next Week Focus
- Reconcile the shared recovery boundary across `repositories/RightNow-AI-autokernel.md`, `repositories/run-llama-liteparse.md`, `repositories/chaindexing-chaindexing-rs.md`, `repositories/openziti-ziti.md`, and `repositories/alireza787b-mavsdk_drone_show.md` so the week’s strongest mechanisms can be compared on one axis: what state is persisted, what gets promoted lazily, and what remains reversible after restart or rollback.
