# Recovery Smoke Fixture

This is deliberately not a valid radar report. It is copied into reports only
by the manual, no-model recovery smoke job. Validation must reject it and the
failure bundle must preserve these exact bytes without publishing a PR.
